# Host-side Python dependency for the config parser (scripts/lib.sh yaml_get)
# and the pipeline/ CLI tools. Everything else (VEP, LOFTEE, bcftools, ...)
# runs inside the container. scripts/setup_environment.sh installs this.
pyyaml
