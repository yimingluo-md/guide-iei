#!/bin/bash
# Source-checkout fallback. macOS release builds replace this file with a
# universal arm64+x86_64 native shim which invokes the same resource script.
set -u
HERE="$(cd "$(dirname "$0")" && pwd)"
exec /bin/bash "${HERE}/../Resources/launcher.sh" "$@"
